{
  "name": "products",
  "$schema": "../node_modules/nx/schemas/project-schema.json",
  "sourceRoot": "products/src",
  "projectType": "application",
  "tags": [],
  "targets": {
    "build": {
      "executor": "@nx/rspack:rspack",
      "outputs": ["{options.outputPath}"],
      "defaultConfiguration": "production",
      "options": {
        "target": "web",
        "outputPath": "dist/products",
        "index": "products/src/index.html",
        "main": "products/src/main.ts",
        "tsConfig": "products/tsconfig.app.json",
        "rspackConfig": "products/rspack.config.ts",
        "assets": ["products/src/favicon.ico", "products/src/assets"]
      },
      "configurations": {
        "development": {
          "mode": "development"
        },
        "production": {
          "mode": "production",
          "optimization": true,
          "sourceMap": false,
          "rspackConfig": "products/rspack.config.prod.ts"
        }
      }
    },
    "serve": {
      "executor": "@nx/rspack:module-federation-dev-server",
      "options": {
        "buildTarget": "products:build:development",
        "port": 4201
      },
      "configurations": {
        "development": {},
        "production": {
          "buildTarget": "products:build:production"
        }
      }
    },
    "lint": {
      "executor": "@nx/eslint:lint"
    },
    "serve-static": {
      "executor": "@nx/rspack:module-federation-static-server",
      "defaultConfiguration": "production",
      "options": {
        "serveTarget": "products:serve"
      },
      "configurations": {
        "development": {
          "serveTarget": "products:serve:development"
        },
        "production": {
          "serveTarget": "products:serve:production"
        }
      }
    },
    "test": {
      "executor": "@nx/jest:jest",
      "outputs": ["{workspaceRoot}/coverage/{projectRoot}"],
      "options": {
        "jestConfig": "products/jest.config.ts"
      }
    }
  }
}
